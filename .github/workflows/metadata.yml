name: metadata

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  metadata-check:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Run dependency-free metadata checks
        run: ./scripts/ae/run_metadata_tests.sh
